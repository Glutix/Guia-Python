"""
    ! Los operadores lógicos son herramientas fundamentales en la programación 
    ! Aquí hay una breve explicación de cada uno de ellos:

    ! AND (Y): 
    Este operador retorna True si ambos operandos son True, de lo contrario, 
    retorna False. Por ejemplo:

    ? True and True retorna True.
    ? True and False retorna False.
    ? False and False retorna False.
    
    ! OR (O): 
    Este operador retorna True si al menos uno de los operandos es True, 
    de lo contrario, retorna False. Por ejemplo:

    ? True or True retorna True.
    ? True or False retorna True.
    ? False or False retorna False.

    ! NOT (NO): 
    Este operador retorna el valor opuesto de su único operando. 
    Si el operando es True, retorna False; si el operando es False, 
    retorna True. Por ejemplo:

    ? not True retorna False.
    ? not False retorna True.

    ! Estos operadores son fundamentales para realizar comparaciones lógicas 
    ! en programas y para controlar el flujo de ejecución de un programa 
    ! en base a condiciones específicas.
"""

